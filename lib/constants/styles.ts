export const ARROW_BTN_RADIUS = 35;

export const ROOT_NODE_RADIUS = 30;
export const GROUP_NODE_RADIUS = 5;
export const REG_NODE_RADIUS = 10;

export const PRESSABLE_OPACITY = 0;

export const NODE_SPACING_FACTOR = 0.4;

export const NODE_BORDER_WIDTH = 0;
export const ROOT_TEXT_SIZE = 40;
export const REG_TEXT_SIZE = 20;

// REVIEW: changing TAB_BAR_HEIGHT will NOT effect ACTUAL tab bar height
// REVIEW: It was hard-coded (after i looked at it) to calculate arrow position
// !TODO: So this is probably not ideal
export const TAB_BAR_HEIGHT = 79;
